# Data Brief 2026-06-23

## Summary

The CR API stream exposed one new capability-bearing event pattern since the last Data Analyst run: `Restless Undead`, surfaced through `/events` as `#2PRC8GLU` and through battle logs as game mode `72000376` / `Event_RestlessDead`.

Issue: https://github.com/jthingelstad/elixir-bot/issues/105

## Evidence

- Last-run boundary: `2026-06-21T07:02:13Z`.
- `api_sentinel_observations` first saw `battle_game_mode` `72000376` at `2026-06-22T10:42:09Z`.
- `api_sentinel_observations` first saw event tag `#2PRC8GLU` at `2026-06-22T11:41:56Z`.
- `/events` title: `Restless Undead`.
- `/events` description: `Tombstones randomly appear around the Arena, with royals waiting to rise once destroyed`.
- `battle_telemetry` has 122 rows for `Event_RestlessDead` across 18 members.
- Outcomes in telemetry: 68 W / 54 L.
- Battle window: `20260622T094554.000Z` through `20260624T013829.000Z`.
- Every telemetry row is `mode_group='special_event'`, `is_special_event=1`, `is_competitive=1`, and `deck_selection='eventDeck'`.

## Members Affected

Top observed participants by battle count:

- kiruba: 26
- gtr0925: 15
- pax: 14
- pigsareus: 11
- 28: 10
- Th15_Guy: 9

## Downstream State

Elixir is capturing enough of the mode to reason about activity and outcomes:

- `game_mode_contexts` stores `#2PRC8GLU` as the `Restless Undead` event context.
- `battle_telemetry` links the battle rows to `event_tag='#2PRC8GLU'` and `game_mode_id=72000376`.
- `member_battle_facts` has 94 derived rows across 14 members.
- `member_daily_battle_rollups` has 94 derived battles across June 22-23.

The product layer still treats it generically:

- Repo search found no prompt, doc, or code reference for `Restless Undead`, `Event_RestlessDead`, `72000376`, or `#2PRC8GLU`.

## Product Intelligence

Restless Undead is a rotating, competitive, same-event-deck special event. This is a useful PM seed because it is not just a new label; the data already supports participation counts, win/loss records, member-level activity, and event-specific rollups.

The PM question is whether fresh rotating event modes should become a recurring product surface for Elixir: detect the mode, characterize clan uptake, and decide when event performance deserves recognition, comparison, or leadership context.

## Other Checks

- New sentinel first-seen rows after the last run were limited to `#2PRC8GLU` and `72000376`.
- No new raw schema paths were first-seen after the last run.
- The current card catalog includes `Boss Bandit`, `Spirit Empress`, and `Vines`, but the local retained card payload history has only one cards snapshot, so this run cannot prove those are new since the prior run.
- First-ever v5 detection types in this window: `path_of_legend_promotion` once and `join_anniversary` once. Both are noted but did not warrant separate issues from this pass.
