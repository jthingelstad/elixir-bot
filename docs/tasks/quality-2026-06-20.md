# Quality Report - 2026-06-20

Role: Elixir Quality Manager
Window: primarily last 7 days, with post-fix verification after `2026-06-20T14:42:56Z`

## Summary

Elixir is live and producing current telemetry through `2026-06-20T16:59:43Z`. Prompt-feedback quality is quiet: there are 0 active thumbs-down records in the last 30 days, and the empty-content prompt-failure fix has no new prompt failures after the latest restart. The main live defect is still routing quality in awareness: `clan_awareness` can turn a low-value `api_schema_sentinel` input into unrelated River Race or leader-lounge posts with empty `covers_signal_keys`.

## Top Failure Modes

1. Empty-cover awareness posts still occur after #85 was closed.
   - Tick `655` at `2026-06-20T15:59:43Z`: `clan_awareness`, `signals_in=1`, `posts_delivered=0`, `posts_rejected=1`, `covered_keys=0`, `all_ok=0`.
   - Tick `657` at `2026-06-20T16:59:43Z`: `clan_awareness`, `signals_in=1`, `posts_delivered=0`, `posts_rejected=2`, `covered_keys=0`, `all_ok=0`.
   - Failed intents `92`, `95`, and `96` all used `covers_signal_keys_json=[]` for `api_schema_sentinel:202606201102:a514cc7d15bb`.
   - The generated summaries were unrelated leader review or River Race copy, not API sentinel handling.

2. Prompt failures are currently stale, not active.
   - `prompt_failures` has 1 record in the last 7 days: arena-relay screenshot truncation on `2026-06-14T21:19:08Z`.
   - No prompt failures were recorded after `2026-06-20T14:42:56Z`.
   - The older blank `messages.N: user messages must have non-empty content` failure has not recurred after #84.

3. Signal delivery is mostly working, with one quality gap.
   - Last 7 days of `communication_intents`: 39 total, 28 delivered, 8 failed, 2 skipped, 1 still planned.
   - The failed intents are concentrated in `leader-lounge` (4) and `river-race` (4), all from empty-cover awareness posts.
   - Delivered activity remains healthy for `member-highlights` (18), `river-race` (4), `clan-events` (3), and `arena-relay` (3).

## Noise And Silence

- Noise risk: awareness continues to draft off-topic leader or River Race posts when the only input is an API schema sentinel. The delivery guard blocks the visible Discord noise, but the agent turn is still wasted and the source signal remains unresolved.
- Silence risk: no hard-fallback failures were observed in the last 7 days. War and progression signals are generally delivered or explicitly skipped.
- Arena-relay cooldown skips appear intentional policy, not a defect in this run.

## Regressions / Fix Verification

- Reopened #85 because the recently closed fixes did not eliminate the live empty-cover family. Fresh evidence was added in issue comment `https://github.com/jthingelstad/elixir-bot/issues/85#issuecomment-4759185931`.
- #84 appears fixed for the original blank-message prompt-failure vector: no post-fix prompt failures and no new `messages.N: user messages must have non-empty content` failures.
- #79 appears fixed for memory timestamp search in current telemetry: no post-fix `offset-naive` / `offset-aware` memory search crash was found in the current post-restart slice.

## Issues Updated

- Reopened #85: `Investigate repeated skipped or failed signal outcomes`
- Added `bug` label to #85 so it routes as a concrete defect.

## Suggested Next Focus

Build Manager should keep #85 focused on preventing awareness from emitting posts that do not cover the current signal batch, especially for low-value sentinel signals that should be silently skipped with a clear reason.
