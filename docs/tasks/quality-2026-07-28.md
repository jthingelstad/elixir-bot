# Quality report — 2026-07-28

Window: 2026-07-21 through 2026-07-28 UTC. This is the first external weekly
improvement-discovery pass replacing Elixir's retired self-evaluation queue
(#209).

## Headline

- Runtime confidence was healthy: no live error groups, no liveness findings,
  and 11 confidence tests passed.
- The read-only post-quality scorecard sampled 37 delivered messages: 30
  awareness posts and 7 interactive messages. Deterministic game accuracy was
  100%, with no repetition or game-accuracy flags.
- The run wrote zero feedback memories and no SQLite suggestion rows. GitHub is
  the only durable work queue.

## Finding

Two consecutive `deck_review` requests on 2026-07-26 were truncated at the
4096-token ceiling and produced no stored assistant reply. Filed #217 with the
two Discord request IDs, exact prompt-failure signature, and a regression
acceptance criterion.

## Leadership feedback

Leaders decided 28 cards in the window: 25 done and 3 rejected. After excluding
two engine auto-withdrawals, all action types were accepted at 100% except
demotion recommendations (1 accepted, 1 rejected). That sample is too small to
support a broad demotion-quality claim; the rejection carried specific member
context and remains a product-discovery input rather than a defect by itself.

Two in-game relay cards were copy-edited, both retaining roughly 90% similarity.
There is no repeated rewrite pattern yet.

## Issues

- #217 — Prevent deck-review truncation from dropping #ask-elixir replies.
