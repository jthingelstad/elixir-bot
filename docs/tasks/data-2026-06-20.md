# Data brief - 2026-06-20

First recorded Data Analyst run. The worktree was not clean
(`docs/tasks/quality-2026-06-20.md` was already untracked), so the repo was not
pulled before analysis. Database reads used SQLite read-only/immutable access.

## Currentness baseline

- `raw_api_payloads` is current for core streams through `2026-06-20T17:57:01Z`.
- `player_profile_snapshots` is current through `2026-06-20T17:29:43Z`.
- `card_catalog` synced 125 cards at `2026-06-20T17:59:51Z`; no sampled player or
  battle card IDs were missing from the catalog.
- `/events` has only one retained raw payload, from `2026-06-13T11:51:40Z`.
- `api_sentinel` runtime status last succeeded at `2026-06-19T20:07:22Z`, despite
  the activity being declared as a 4-hour cadence.

## Findings

### Princess/Anarchy event surface

Filed as https://github.com/jthingelstad/elixir-bot/issues/90.

The stream has a current special-event surface worth Product Manager review:

- `/events` includes `Princess Gambit` (`#2PRC9GU0`) and `Merge Tactics`
  (`#2PRCGRYL`) with null descriptions.
- `member_battle_facts` has 940 deduped `All_Random_Princess` battles
  (`game_mode_id=72000501`, `battle_type=trail`, `mode_group=special_event`)
  across 29 members from `20260610T095732.000Z` to `20260620T045706.000Z`.
- Daily `All_Random_Princess` volume peaked at 207 battles from 13 members on
  2026-06-10 and remained active through 2026-06-20.
- `AnarchyLeagueCompletion` first appeared in player badges at
  `2026-06-20T11:02:55Z`; 9 raw player payload rows covered 5 distinct members.
- The badge generated 5 `badge_earned` events between `2026-06-20T11:02:57Z`
  and `2026-06-20T15:59:45Z`. Fields were `level=1`, `maxLevel=10`,
  `progress=1`, `target=2`.
- Those 5 badge members all had recent `All_Random_Princess` activity: 52, 50,
  47, 46, and 37 battles since 2026-06-10.
- Downstream currently categorizes the badge as `general`, so Elixir recognizes
  it generically instead of as a named event/seasonal surface.

### Active event context ingest gap

Filed as https://github.com/jthingelstad/elixir-bot/issues/91.

The live event context path is not producing queryable active-event context:

- `game_mode_contexts` has 0 rows.
- `get_clan_game_modes(aspect="events")` exposes `game_mode_contexts` as
  `active_events`, so active `/events` entries are unavailable through that
  structured tool surface.
- `raw_api_payloads` has one `events` payload with 8 items, and
  `api_sentinel_observations` has 8 `event` observations last touched at
  `2026-06-19T20:07:22Z`.
- Route to Operations first because the scheduled `api-sentinel` activity also
  appears stale. If the scheduler/runtime is healthy, the remaining issue is a
  Build-owned persistence/derivation defect.

## Non-findings

- `DoubleElixir_Friendly` (`72000011`) was first observed on 2026-06-18 but is
  only one friendly match duplicated across two members, not a meaningful new
  capability surface.
- `event_rollups` is empty, but the battle event stream is still inside its
  90-day retention window, so this is not actionable from this pass.
- Clan daily rollup rows can show `completeness_ratio=1.0` while
  `is_complete=0`; this appears consistent with stricter member-level
  completeness semantics rather than a clear data defect.
